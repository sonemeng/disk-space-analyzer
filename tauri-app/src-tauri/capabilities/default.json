{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the disk analyzer",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
